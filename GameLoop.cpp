#include <iostream>

void mainLoop(){
  bool play = true;
  int turn = 1;
  while (play){
  //   if (ai)
  }
}

std::string playerShooting(){
  std::string shotPoint;
  std::cout << std::endl << "Please enter coordinate to fire at: ";
  std::cin >> shotPoint;
  return shotPoint;
}