#include <random>

class helpers{
  public:
    ///generateNumber generates a number between 1 and maxValue thats passed in. Returns the number generated
    static int generateNumber(int maxValue);
};
